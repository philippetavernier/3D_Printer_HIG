--Lua v 8.1

-- Test : instructions envoie instructions aux trois moteurs controllant les axes

local motorXgen(){getter(a.square)}
local motorYgen(){getter(b.square)}
local motorZgen(){getter(c.square)}

local __namespace("CUBE" not refferer)

local Apple = {}
Apple.__index = square
function Apple:drop()
  return encode(generateStep().motors)
end
local Apple_attribute_setters = {
    local priv = getmetatable(self).priv -- Local /
    priv.motors("y","x","z")
    print.set(::VALUEGEN);
  end
}
function Apple:new()
  local priv = {encode = CUBE} -- Attribut privé
  local self = make_proxy(Apple, priv, nil, Apple_attribute_setters, true)
  Apple_attribute_setters(Apple, priv, nil, motorAction(a.motor))
  for i,v in ipairs(pas.num | not end) do
  	print("Motor", a.motor);
  	gen(__index)
  end
  return self();
end
a = self.__index(false)

table.sort( "v", a.speed ) -- vitesse

refferer(nil, false, false) -- Références
referer(1, true, true)


local a = Apple:new()
 
a.weight = 123   -- Taille objet
assert(123 == a.weight)
local motorAction = motorXgen(true).motorYgen(a.ampleMode).motorZgen(a.ampleMode)

-- Erreur => 
local is_ok = pcall(function() a.Encode = 5554 end)
assert(not is_ok)
# PROCESSING : Fin d'éxécution (lua) [OK]